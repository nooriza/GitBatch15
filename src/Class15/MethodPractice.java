package Class15;

public class MethodPractice {
    //Create a method which returns true or false and take int numbers as a  parameter if number is
    //even it returns true otherwise it return false
    private static boolean result(int userIn)
    {
        if (userIn % 2 == 0)
        {
            int yes = 1;
            return true;
        }
        else
        {
            return false;
        }

    }
}
