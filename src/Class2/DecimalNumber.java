package Class2;

public class DecimalNumber {
    public static void main(String[] args) {
        /*
        if you want to store decimal numbers in computer's memory using java we can use following types of
        boxes
        1) Float
        2) Decimal
         */
        float myNumberBox=10.5f;
        float mySecondNumberBox=5.4f;
        double largeDecimalBox=1333333.343;
        System.out.println(largeDecimalBox);
        /* If we want to store characters (#,$,o,s,4) we use char datatype */
        char letter='s';
        char gender='M';
        char symbol='#';
        /* String date type to a different category called non-primitives that is why
        for String rules are a little different for example for S of String is upper case its color is also
         not blue we will discuss these details later on*/
        String name="sam";
        boolean hungry=true;//true means yes
        boolean sleepy=false;//false means no




    }
}
